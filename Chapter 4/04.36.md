# Exercise 4.36
> Assuming `i` is an `int` and `d` is a `double` write the expression `i *= d` so that it does integral, rather than floating-point, multiplication.

## Solution

In order to perform integer multiplication of the expression `i * d` the variable `d` has to be converted to `int`: `i *= static_cast<int>(d)`.
