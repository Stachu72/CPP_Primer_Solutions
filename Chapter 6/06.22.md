# Exercise 6.22
> Write a function to swap two int pointers.

## Solution

- **Function Code:**
  ```c++
  void swapPointers(int *&p1, int *&p2) {
      int *temp = p1;
      p1 = p2;
      p2 = temp;
  }
  ```
  The key to swapping two pointers is passing them by reference.
