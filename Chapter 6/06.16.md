# Exercise 6.16
> The following function, although legal, is less useful than it might be. Identify and correct the limitation on this function:

```c++
bool is_empty(string& s) { return s.empty(); }
```

## Solution

The function does not modify the parameter, so the `s` parameter should be const. Otherwise, the function cannot be called with a `const string`.
