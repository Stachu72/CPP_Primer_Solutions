# Exercise 2.13
> What is the value of j in the following program?

```c++
int i = 42;
int main()
{
    int i = 100;
    int j = i;
}
```

## Solution

The variable `i` in the scope block overshadows global variable, so `j` takes its value from variable, which holds the value `100`.
