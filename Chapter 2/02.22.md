# Exercise 2.22
> Assuming p is a pointer to int, explain the following code:

```c++
if (p) // ...
if (*p) // ...
```

## Solution

- `if (p)` checks whether the pointer is a null pointer,
- `if (*p)` checks whether the pointed object value is different from 0.
