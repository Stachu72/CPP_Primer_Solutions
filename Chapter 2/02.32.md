# Exercise 2.32
> Is the following code legal or not? If not, how might you make it legal?


``` c++
int null = 0, *p = null;
```

## Solution

It is incorrect. The `p` pointer points to the memory of objects. Add `&` before `null`. We cannot use the word `null` as a variable name if it is written in uppercase i.e. `NULL`. Several fixes:
