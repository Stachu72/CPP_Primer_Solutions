# Exercise 2.20
> What does the following program do?

```c++
int i = 42;
int *p1 = &i;
*p1 = *p1 * *p1;
```

## Solution

The program calculates the square of the variable `i` indirectly. The value of `*p1` / `i` is now equal to `1764`.
