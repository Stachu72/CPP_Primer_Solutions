# Exercise 2.6
> What, if any, are the differences between the following definitions:

```c++
int month = 9, day = 7;
int month = 09, day = 07;
```

## Solution

In both lines values are initialized to variables, but the second line uses octal notation. This means that `09` doesn't exist. It should be `011`.
