# Exercise 2.23
> Given a pointer p, can you determine whether p points to a valid object? If so, how? If not, why not?

## Solution

There is no a 100% reliable way to verify whether a pointer is valid, since it holds only raw memory addresses without any metadata that could indicate an invalidity. 
